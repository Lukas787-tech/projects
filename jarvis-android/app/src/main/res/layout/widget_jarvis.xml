<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="10dp"
    android:paddingEnd="8dp">

    <ImageView
        android:id="@+id/widget_core"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:contentDescription="@string/shortcut_talk"
        android:src="@drawable/ic_launcher_foreground" />

    <LinearLayout
        android:id="@+id/widget_talk_area"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:paddingStart="8dp">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/widget_title"
            android:textColor="#FFF2F5F9"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/widget_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:letterSpacing="0.12"
            android:text="@string/widget_subtitle"
            android:textAllCaps="true"
            android:textColor="#FF4FD8FF"
            android:textSize="10sp" />
    </LinearLayout>

    <ImageView
        android:id="@+id/widget_type"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginEnd="6dp"
        android:background="@drawable/widget_button"
        android:contentDescription="@string/shortcut_type"
        android:padding="8dp"
        android:src="@drawable/ic_type" />

    <ImageView
        android:id="@+id/widget_scan"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginEnd="6dp"
        android:background="@drawable/widget_button"
        android:contentDescription="@string/shortcut_scan"
        android:padding="8dp"
        android:src="@drawable/ic_scan" />

    <ImageView
        android:id="@+id/widget_talk"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/widget_button"
        android:contentDescription="@string/shortcut_talk"
        android:padding="8dp"
        android:src="@drawable/ic_talk" />
</LinearLayout>
