<?xml version="1.0" encoding="utf-8"?>
<!--
  Kept as narrow as the job allows.

  Only window-state and window-content changes, only from the three messaging
  apps this can start a conversation in, and no flag that would let it read text
  back. The service itself does nothing at all unless a send was armed seconds
  earlier, so even within that list it is inert almost all of the time.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:packageNames="com.whatsapp,org.telegram.messenger,org.thoughtcrime.securesms"
    android:description="@string/tapper_description" />
