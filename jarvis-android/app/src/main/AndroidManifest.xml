<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Coarse alone is enough for "what is near me"; fine sharpens the route start. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- The floating dot. Android grants this only from its own settings page. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Reading which devices are paired; connecting one stays the system's job. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- "What have I got on today". Read only: events are written by handing the
         insert intent to the user's own calendar app, which needs no permission. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <!-- "Call Anna" needs a number to dial. Read only, and the call is never placed. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Texts are sent outright rather than drafted, which is the whole point
         of asking an assistant to send one. -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- Placing a call rather than only dialling it. Off unless switched on. -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- Alarms and timers are set through the clock app rather than scheduled here. -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <!-- The flash, driven through CameraManager, which needs no camera permission. -->
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />

    <!-- Lets the app resolve the on-device speech recognition / TTS services on API 30+. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- "Open Spotify": the launchable apps, which API 30+ hides by default. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Dialler, messaging, email, browser and calendar handovers. -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSERT" />
            <data android:mimeType="vnd.android.cursor.dir/event" />
        </intent>
        <!-- The camera, for "what is this" and for opening the camera app. -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.media.action.STILL_IMAGE_CAMERA" />
        </intent>
        <!-- Lets the app tell whether any maps app can take a handover. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>

    <application
        android:name=".JarvisApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Jarvis"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Jarvis">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Long-press the power button / assistant gesture opens Jarvis. -->
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VOICE_COMMAND" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- Holds the floating dot over other apps and runs a spoken turn
             without the app itself coming to the front. -->
        <!-- Invisible, lives for a second: raises a permission dialog from a
             tool that is running nowhere near an activity. -->
        <activity
            android:name=".control.AskPermissionActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Presses send in a messaging app, and only when armed to. -->
        <service
            android:name=".control.Tapper"
            android:exported="false"
            android:label="Jarvis"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/tapper" />
        </service>

        <!-- Answers a message in its own notification, the way a watch does.
             The system grants this only from its notification-access page. -->
        <service
            android:name=".notify.ReplyListener"
            android:exported="false"
            android:label="Jarvis"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".overlay.BubbleService"
            android:exported="false"
            android:foregroundServiceType="microphone" />


        <service
            android:name=".voice.WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <receiver
            android:name=".notify.ReminderReceiver"
            android:exported="false" />

        <!-- A routine's daily time: posts the one-tap notification. -->
        <receiver
            android:name=".auto.RoutineReceiver"
            android:exported="false" />

        <!-- Hands the camera app a file of ours to write the full-size photo into. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".notify.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
